<html>
    <head>
        <title> {{ title }} </title>
        {% include 'style.jinja' %}
    </head>
    <body>
        <h1> {{ bill.id }} </h1>
        <h2> {{ bill.description }} </h2>
        <div class="table">
            <div class="table-row">
                <div class="table-cell">
                    {{ before.version }}
                </div>
                <div class="table-cell">
                    {{ after.version }}
                </div>
            </div>
        </div>
        <div id="diff">
          {% autoescape false %} 
            {{ diff }}
          {% endautoescape %} 
        </div>
    </body>
</html>
